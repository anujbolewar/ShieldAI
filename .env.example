# Environment variables for SHIELD AI
# Copy this file to .env and fill in values as needed.
# All variables have sensible defaults — only override what you need.

# ── Data directories ──────────────────────────────────────────
CETP_DATA_DIR=data/cetp
FACTORY_DATA_DIR=data/factories
ALERT_LOG_PATH=data/alerts/evidence_log.jsonl

# ── Anomaly detection ─────────────────────────────────────────
# Empirical COD baseline from priya_cetp_i.csv (Feb 2026)
COD_BASELINE=193.0

# NOTE: Set to 200 for demo (real data max = 230.8 mg/L, 206 breach rows). Raise to 450+ for production.
COD_THRESHOLD=200.0

# ── Temporal backtracking ─────────────────────────────────────
# NOTE: Fixed constant for v1 — will become dynamic in v2.
PIPE_TRAVEL_MINUTES=15
ASOF_TOLERANCE_SECONDS=120

# ── v2 Anti-cheat thresholds ──────────────────────────────────
ZERO_VARIANCE_MINUTES=5
COD_DROP_FRACTION=0.80
TSS_STABLE_FRACTION=0.20

# ── Alert dispatch ────────────────────────────────────────────
# Leave blank to disable webhook
SHIELD_WEBHOOK_URL=

# ── Email (Phase 3) ───────────────────────────────────────────
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
ALERT_EMAIL_TO=
